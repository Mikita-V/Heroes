@using MVCPL.Models
@using MVCPL.Util.Extensions
@using MVCPL.Util.HtmlHelpers
@model IEnumerable<UserViewModel>

@{
    ViewBag.Title = "Index";
    //Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<h2>Index</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
@Html.Partial("_CreatePartial", new UserViewModel())

@Html.Partial("_UsersTable", Model)
@Ajax.ActionLink("Update", "Index", new {}, new AjaxOptions{HttpMethod = "GET", UpdateTargetId = "usersTable", InsertionMode = InsertionMode.Replace}, new { @class = "btn btn-lg btn-success" })
@*<button id="updateUsersTable" class="btn btn-lg btn-success">
    <i class="glyphicon glyphicon-refresh"></i>
    Update
</button>*@

@*TODO: Refactor*@
<div>
    @{
        <h3>Session Info:</h3>
        <br />

        var createdUsers = ViewBag.CreatedUsers as List<UserViewModel>;
        if (createdUsers != null)
        {
            <ul>
                Created Users:
                @foreach (var user in createdUsers)
                {
                    <li>@user.Name / @user.Age</li>
                }
            </ul>
        }

        var updatedUsers = ViewBag.UpdatedUsers as List<UserViewModel>;
        if (updatedUsers != null)
        {
            <ul>
                Updated Users:
                @foreach (var user in updatedUsers)
                {
                    <li>@user.Name / @user.Age</li>
                }
            </ul>
        }

        var deletedUsers = ViewBag.DeletedUsers as List<int>;
        if (deletedUsers != null)
        {
            <ul>
                Deleted Users:
                @foreach (var id in deletedUsers)
                {
                    <li>@id</li>
                }
            </ul>
        }

        if (createdUsers != null || updatedUsers != null || deletedUsers != null)
        {
            <div>
                @Html.ActionLink("Apply Changes", "ApplyChanges", new { apply = true }) |
                @Html.ActionLink("Discard Changes", "ApplyChanges", new { apply = false })
            </div>
        }

    }
</div>

@section Scripts{
    @Scripts.Render("~/bundles/ajax")

    <script>
        function editFailed(result) {
            //$("span[data-valmsg-for]").text('');
            var errorsInfo = result;
            for (var i = 0; i < result.length; ++i) {
                var key = errorsInfo[i].key;
                var message = errorsInfo[i].errors[0];
                //for (var j = 0; j < errorsInfo[i].errors.length; ++j) {
                //    message += (errorsInfo[i].errors[j] + "; ");
                //}

                $("span[data-valmsg-for='" + key + "']").text(message);
            }
        }
    </script>
    
    <script type="text/javascript">
        //TODO: refactor
        $('#formid').ajaxForm(function (data) {
            $("span[data-valmsg-for]").text('');
            if (typeof data === 'string') {
                $('#usersTableBody').append(data);
            } else {
                editFailed(data);
            }
        });

    </script>

    @*<script>
        $("#btnCreateUser").click(function () {

            var formData = new FormData();
            var file = document.getElementById("file").files[0];
            formData.append("Photo", file);
            formData.append("Name", $('#Name').val());
            formData.append("BirthDate", $('#BirthDate').val());

            $.ajax({
                url: '@Url.Action("Create")',
                type: "POST",
                dataType: "JSON",
                data: formData,
                contentType: false,
                processData: false,
                error: function (result) {
                    alert("Error!");
                    //editFailed(result);
                },
                success: function (result) {
                    alert("Success!");
                    //editFailed(result);
                }
            });

        });
    </script>*@
}
